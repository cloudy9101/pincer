{
  "name": "pincer-gateway",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "cd admin && bun run build && cd .. && wrangler d1 migrations apply DB --remote && wrangler deploy",
    "deploy:worker": "wrangler d1 migrations apply DB --remote && wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "migrate:remote": "wrangler d1 migrations apply DB --remote",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/mcp": "^1.0.21",
    "ai": "^6.0.85",
    "croner": "^10.0.1",
    "workers-ai-provider": "^3.1.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260213.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.65.0"
  }
}
